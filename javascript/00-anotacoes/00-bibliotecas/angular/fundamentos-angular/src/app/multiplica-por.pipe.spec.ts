import { MultiplicaPorPipe } from './multiplica-por.pipe';

describe('MultiplicaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
